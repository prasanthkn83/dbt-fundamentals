version: 2

sources:
    - name: snowplow
      database: raw
      loaded_at_field: collector_tstamp
      tables:
        - name: events          
          freshness:
            error_after: {count: 1, period: hour}